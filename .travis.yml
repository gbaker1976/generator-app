language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-app' ]; then cd .. && eval "mv $currentfolder generator-app" && cd generator-app; fi

